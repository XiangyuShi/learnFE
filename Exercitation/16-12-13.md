#Exercitation
~~~

3.
CI测试环境部署，上线后 测试环境需要更新版本号，否则不能正确部署
PPE和Product 在Rollout部署  部署前需release后端war包
CI 只能部署一个分支，部署前需删除原分支

4.react项目
npm install
sudo npm install -g webpack
sudo npm install -g webpack-dev-server
npm run dev
node webapi/index
